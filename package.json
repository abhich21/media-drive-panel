{
  "name": "mdm-dashboard",
  "version": "1.0.0",
  "description": "Media Drive Management Dashboard",
  "scripts": {
    "build:css": "npx tailwindcss -i ./input.css -o ./assets/css/styles.css --watch",
    "build": "npx tailwindcss -i ./input.css -o ./assets/css/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
